# RUN PACK · US-22
generated 2026-08-28 10:17Z · HEAD 429487b

You are executing ONE story inside its scope. Obey every law below.
Do not claim done without reporting verification evidence for each AC and DoD.
Do not edit files outside your scope. Do not invent work beyond this pack.

## Write-set
Paths are relative to this working-copy root.
You may write:
- src/
- archive/evidence/
- archive/packs/
This story's status may be set to in_progress, review, or blocked.
Status done is forbidden.

## Do-not-write
- METHOD.md
- bin/

## Story US-22 · Optional scope.touch and scope.forbid on stories
- epic E5 · priority p1 · status in_progress
- As a operator packing an implementation story, I want an optional scope object with touch and forbid path-prefix arrays so that pack prints the paths the agent may edit, and those prefixes stay valid in a later git worktree of the same repo.

### Acceptance criteria (each needs evidence)
- [ ] AC-1: AMENDMENTS.md records optional schema field scope with keys touch and forbid, each an array of path prefixes
- [ ] AC-2: METHOD.md states scope prefixes are relative to the working-copy root
- [ ] AC-3: A story object without scope still gets bin/gate exit 0 (no BLOCKER)
- [ ] AC-4: bin/gate prints MINOR when status is backlog or ready and scope is missing or both arrays are empty
- [ ] AC-5: bin/pack prints a Scope section listing each touch prefix and each forbid prefix when present
- [ ] AC-6: bin/pack prints 'Scope: unset' when the field is absent
- [ ] AC-7: bin/verify exits 0 on this repo while existing stories have no scope key

### Definition of Done
- [ ] DoD-1: Stdlib Python only
- [ ] DoD-2: METHOD.md §2 story schema line names the optional scope field
- [ ] DoD-3: Accepted via accept flow with evidence list

## Laws

### law/stack.md
# stack

Versioned toolchain constraints. Packs bind execution to this file.

## Toolchain

- `bin/` is stdlib Python only.
- No pip installs.
- No build step.

## Artifacts

- Canonical store is markdown and JSON files in git.
- No external database.

## Runtime

- Git is the only runtime dependency.
- Git inspection commands run with `--no-pager` (or `GIT_PAGER=cat`); agent shells have no TTY.


## Resume context
US-21 accepted. Next: US-22 optional scope. Remaining ready: US-24, US-25, US-23, US-22. US-18 unsigned.
