# RUN PACK · US-16
generated 2026-08-25 19:19Z · HEAD 0328248

You are executing ONE story inside its scope. Obey every law below.
Do not claim done without reporting verification evidence for each AC and DoD.
Do not edit files outside your scope. Do not invent work beyond this pack.

## Story US-16 · Overview v2 — human review ergonomics
- epic E5 · priority p1 · status ready
- As a operator reviewing delivered work and deciding acceptance, I want the overview page to carry everything an acceptance decision needs onto one screen so that human review is faster than opening files, and human+machine delivery is truly optimized.

### Acceptance criteria (each needs evidence)
- [ ] AC-1: Story details render AC and DoD items as interactive checkboxes so a reviewer can tick progress while reading; checkbox state is session-local and never persisted
- [ ] AC-2: Stories in review status appear in the Needs-you section with a relative link to their newest evidence bundle and its first three lines quoted inline
- [ ] AC-3: Backlog stories display a gate chip computed at render time showing PASS or the number of open issues, using the same checks as bin/gate
- [ ] AC-4: Stories with archived packs or evidence bundles under archive/ show clickable relative links to those artifact paths
- [ ] AC-5: The page keeps its existing section set - header, Needs-you, Progress, In-flight, All stories, Recent decisions, Resume cue - and adds no new top-level sections
- [ ] AC-6: The page stays one self-contained HTML file with zero JavaScript and opens from file://

### Definition of Done
- [ ] DoD-1: Gate logic is reused from the bin/gate implementation rather than duplicated
- [ ] DoD-2: bin/render runs clean on this repo and bin/verify stays green afterwards
- [ ] DoD-3: Accepted via accept flow with evidence list

## Laws

### law/stack.md
# stack

Versioned toolchain constraints. Packs bind execution to this file.

## Toolchain

- `bin/` is stdlib Python only.
- No pip installs.
- No build step.

## Artifacts

- Canonical store is markdown and JSON files in git.
- No external database.

## Runtime

- Git is the only runtime dependency.
- Git inspection commands run with `--no-pager` (or `GIT_PAGER=cat`); agent shells have no TTY.


## Resume context
P3: pick one staff member + one real project, hand over README + root adapters, local-only start; exit = first staff end-to-end story without private instruction. P4: draft skills/story-workshop SKILL.md v0 (elicitation tree → gate → sign), canonical example = US-14 ending.
