# RUN PACK · US-17
generated 2026-08-25 19:51Z · HEAD 7b4431e

You are executing ONE story inside its scope. Obey every law below.
Do not claim done without reporting verification evidence for each AC and DoD.
Do not edit files outside your scope. Do not invent work beyond this pack.

## Story US-17 · bin/init bootstraps a local git repository when none exists
- epic E5 · priority p1 · status ready
- As a staff member who downloaded the keel repo as a ZIP archive, I want bin/init to leave me with a working local git repository without extra steps so that the method commit discipline and agent bootstrap work even when I skipped cloning.

### Acceptance criteria (each needs evidence)
- [ ] AC-1: When .git is absent, bin/init creates a local git repository with branch main before resetting artifacts and prints that it entered local-only mode
- [ ] AC-2: After init on a ZIP-style folder, git status executes successfully and lists the workspace files as untracked
- [ ] AC-3: When .git already exists, init behaves exactly as before with no git init call
- [ ] AC-4: The double-run guard is unchanged: second run without --force exits non-zero naming --force
- [ ] AC-5: bin/init replaces PLAN.md with a blank project-plan stub carrying no keel build phases
- [ ] AC-6: bin/init replaces README.md with an instance edition that keeps the usage sections (start, loop, tools, layout, governance) but strips keel status and build history
- [ ] AC-7: After init, no file outside archive-less method tooling contains references to keel build stories US-01 to US-17, decision IDs D-nn, failure IDs F-nn, or amendment IDs A-nn

### Definition of Done
- [ ] DoD-1: Uses the existing subprocess-based git helper pattern, stdlib only
- [ ] DoD-2: Template texts live as constants in one place so upstream improvements ship via pulls
- [ ] DoD-3: README documents the ZIP path alongside clone and template options
- [ ] DoD-4: Accepted via accept flow with evidence list

## Laws

### law/stack.md
# stack

Versioned toolchain constraints. Packs bind execution to this file.

## Toolchain

- `bin/` is stdlib Python only.
- No pip installs.
- No build step.

## Artifacts

- Canonical store is markdown and JSON files in git.
- No external database.

## Runtime

- Git is the only runtime dependency.
- Git inspection commands run with `--no-pager` (or `GIT_PAGER=cat`); agent shells have no TTY.


## Resume context
P3 OPEN: pick one staff member + one real client project. They clone keel, run bin/init, work local-only with Codex/OpenCode using skills/story-workshop for intents. Exit: first end-to-end staff story without private instruction.
