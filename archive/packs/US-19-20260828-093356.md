# RUN PACK · US-19
generated 2026-08-28 09:33Z · HEAD c7304a0

You are executing ONE story inside its scope. Obey every law below.
Do not claim done without reporting verification evidence for each AC and DoD.
Do not edit files outside your scope. Do not invent work beyond this pack.

## Story US-19 · VERSION file is the single version string
- epic E5 · priority p0 · status ready
- As a operator or agent running verify, I want METHOD.md and bin/boot to carry the same version as the VERSION file so that a clone cannot report two different keel versions in one session.

### Acceptance criteria (each needs evidence)
- [ ] AC-1: VERSION remains one line matching ^\d+\.\d+\.\d+$
- [ ] AC-2: bin/boot stdout contains the trimmed VERSION file text after the substring 'keel v'
- [ ] AC-3: METHOD.md contains that same VERSION text on its Status line
- [ ] AC-4: bin/verify prints FAIL and exits 1 if METHOD.md Status line does not contain the VERSION text
- [ ] AC-5: bin/verify prints FAIL and exits 1 if bin/boot stdout would not contain 'keel v' plus the VERSION text

### Definition of Done
- [ ] DoD-1: Stdlib Python only, no pip installs
- [ ] DoD-2: Mismatch case proven by a temporary VERSION edit in evidence, then restored
- [ ] DoD-3: Accepted via accept flow with evidence list

## Laws

### law/stack.md
# stack

Versioned toolchain constraints. Packs bind execution to this file.

## Toolchain

- `bin/` is stdlib Python only.
- No pip installs.
- No build step.

## Artifacts

- Canonical store is markdown and JSON files in git.
- No external database.

## Runtime

- Git is the only runtime dependency.
- Git inspection commands run with `--no-pager` (or `GIT_PAGER=cat`); agent shells have no TTY.


## Resume context
P3 staff rollout complete. Ghost-fix batch ready in sign order: US-19, US-24, US-25, US-20, US-21, US-23, US-22. US-18 unsigned. Logo slot reserved.
